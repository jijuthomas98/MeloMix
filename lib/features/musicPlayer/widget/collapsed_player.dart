import 'package:flutter/material.dart';

class CollapsedPlayer extends StatelessWidget {
  const CollapsedPlayer({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.amber,
      child: Text('Mini player'),
    );
  }
}
